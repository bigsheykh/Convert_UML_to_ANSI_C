#include "overload.h"
void Map::set_point_6xLE8tyZB(int index, int x, int y)
{
	this->points[index].x = x;
	this->points[index].y = y;
}

class Point Map::get_point_pJ0cdCxyo(int index)
{
    class Point a;
    a.x = this->points[index].x;
    a.y = this->points[index].y;
	return a;
}

